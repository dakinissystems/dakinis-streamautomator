{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "npm run search:reindex -- --limit=1000",
    "cronSchedule": "0 4 * * *",
    "restartPolicyType": "NEVER"
  }
}

{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "20"
  },
  "steps": {
    "install": {
      "commands": ["npm ci --omit=dev"]
    }
  },
  "deploy": {
    "startCommand": "npm run migrate && node src/apiServer.js"
  }
}
